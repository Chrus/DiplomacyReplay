<Window x:Class="DiplomacyReplay.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DiplomacyReplay"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800"
        WindowState="Maximized">
    <Window.Resources>
        
    </Window.Resources>
    <Grid>
        <TabControl TabStripPlacement="Bottom">
            <TabItem Header="Home">
                <Frame Name="HomeFrame">
                    <Frame.Content>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="50"/>
                                <ColumnDefinition Width="300"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>


                            <Border BorderBrush="Red" BorderThickness="5" Grid.Column="1" Grid.ColumnSpan="2" Focusable="True">
                                <Canvas Background="CornflowerBlue" Margin="10" Focusable="True">
                                </Canvas>
                            </Border>


                            <local:SidebarControl x:Name="sidebar" Grid.Column="0" Grid.ColumnSpan="2" Background="Orange">
                                <local:SidebarItem Header="Test">
                                    <TextBlock Text="ONE"/>
                                </local:SidebarItem>
                                <local:SidebarItem Header="Test2">
                                    <TextBlock Text="TWO"/>
                                </local:SidebarItem>
                            </local:SidebarControl>
                        </Grid>
                    </Frame.Content>
                </Frame>
            </TabItem>

            <TabItem Name="MapTabHeader" Header="Map">
            </TabItem>

            <TabItem Header="Timeline"/>

            <TabItem Header="Animation"/>

        </TabControl>
    </Grid>
</Window>
