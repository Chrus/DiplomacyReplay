<Page x:Class="DiplomacyReplay.AnimationPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:DiplomacyReplay"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AnimationPage">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="50"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border BorderBrush="Red" BorderThickness="5" Grid.Column="1" Grid.ColumnSpan="2" Background="CornflowerBlue">
            <TextBlock Text="ANIMATION PLACEHOLDER" Background="OrangeRed"
                           HorizontalAlignment="Center" VerticalAlignment="Center"
                           FontSize="50"/>
        </Border>

        <local:SidebarControl x:Name="sidebar" Grid.Column="0" Grid.ColumnSpan="2" Background="Orange">
            <local:SidebarItem Header="Assets">
                <TextBlock Text="ONE"/>
            </local:SidebarItem>
            <local:SidebarItem Header="Display">
                <TextBlock Text="TWO"/>
            </local:SidebarItem>
        </local:SidebarControl>
    </Grid>
</Page>
